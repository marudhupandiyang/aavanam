<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="public/style.css">
  </head>
  <body>
    <div class="left-menu">
      <% for(var i=0; i < classes.length; i += 1) {%>
        <div class="cls"><%= classes[i].name %></div>
      <% } %>
    </div>
    <div class="content">
      <%= content %>
    </div>
  </body>
</html>
